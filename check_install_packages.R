install.packages("pacman")

#pacman is going to check if we have the packages and if not it is going to install it (and also load)
pacman::p_load(tidyverse, data.table, readODS, tools, lubridate, janitor)
